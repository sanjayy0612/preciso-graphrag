[pytest]
testpaths = tests
asyncio_mode = auto
# Suppress the harmless zero-vector normalization warning from the offline
# fallback embedder (returns all-zero vectors by design).
filterwarnings =
    ignore:invalid value encountered in divide:RuntimeWarning
